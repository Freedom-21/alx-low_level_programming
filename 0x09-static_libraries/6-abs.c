#include "main.h"
/**
 * _abs - function to compute absolute value
 * @n: number to be computed
 * Return: 0 for success
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
